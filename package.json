{
  "name": "scs-backend",
  "version": "1.0.0",
  "description": "Special Cargo Services — single-file backend (site + AI Cargo Assistant + RFQ notifications)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.22.0"
  }
}
